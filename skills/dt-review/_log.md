Append-only friction notes. One line per invocation that hit friction.
2026-05-27 dt-review: invoke-codex-round.ps1 passed a large embedded prompt as a command-line argument on Windows and hit "The command line is too long"; stdin invocation with codex.cmd worked.
2026-05-28 dt-review: codex exec hangs/throws "StandardOutputEncoding is only supported when standard output is redirected" when launched from the Claude PowerShell tool (redirected, no-console host) - reproduced with both the npm codex.cmd shim and the native vendor codex.exe; one trivial-prompt test hung ~70min. The Bash tool gives codex a clean stdio env and works. Ran all 3 rounds via Bash (codex exec --model gpt-5.5 -c model_reasoning_effort=medium, prompt piped on stdin). The stdin pipe (now in invoke-codex-round.ps1) likely also closes stdin and may fix the pwsh hang - unverified this run. Consider documenting a Bash-driven fallback / verifying the script under the pwsh tool.
2026-05-28 dt-review RESOLVED in 0.9.26/0.9.27: stdin pipe in invoke-codex-round.ps1 fixed both the arg-length overflow and the PowerShell-tool hang (verified - the fixed script runs clean under the PowerShell tool in ~15s, not just Bash). SKILL.md Execution-model section mandates Bash + `pwsh -NoProfile -File` + per-step timeout; -NoProfile is the only deterministic guard against the separate ConstrainedLanguage profile crash, so Bash stays the canonical path. All tiers default to medium reasoning effort to override the global model_reasoning_effort=high.
2026-05-28 dt-review: a prior run failed with "gpt-5.1-codex-max model is not supported when using Codex with a ChatGPT account." Diagnosis - that run bypassed the scripts and hand-rolled codex calls (codex-out-p1a.txt/p1b) without --model, so it inherited the config default, which was the now-API-only gpt-5.1-codex-max. Live re-probe confirmed both current pins (gpt-5.5, gpt-5.3-codex) return OK. RESOLVED in 0.9.29: new shared scripts/resolve-codex-model.ps1 self-heals the pin against ~/.codex/models_cache.json (prefer pin -> per-tier fallback list -> throw with selectable list); preflight + invoke-codex-round gain -Tier and call it. VERIFIED 2026-05-28: resolver unit suite 10/10 pass (healthy-pin both tiers, dead-pin fallback both tiers, hidden-pin skip, missing/unparseable/zero-selectable soft-degrade, none-usable THROW with correct selectable list, no-pin tier-head); live preflight-codex.ps1 -Tier light ran clean (exit 0, resolved gpt-5.3-codex, returned OK). Tests written to a results file because the Bash tool's stdout rendering intermittently returned empty mid-session - read the file, do not trust a blank Bash result.
2026-07-12 dt-review: Codex rejected review-output-schema.json because structured outputs do not permit uniqueItems; moved candidate-dimension uniqueness enforcement into invoke-codex-round.ps1 semantic validation.
2026-07-12 dt-review: light review required repeated cap extensions because Rounds 3 and 4 surfaced one new low blocking coherence issue at a time, despite Round 4 explicitly instructing the reviewer to surface every establishable issue now rather than serialize findings.
