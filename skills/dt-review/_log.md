Append-only friction notes. One line per invocation that hit friction.
2026-05-27 dt-review: invoke-codex-round.ps1 passed a large embedded prompt as a command-line argument on Windows and hit "The command line is too long"; stdin invocation with codex.cmd worked.
2026-05-28 dt-review: codex exec hangs/throws "StandardOutputEncoding is only supported when standard output is redirected" when launched from the Claude PowerShell tool (redirected, no-console host) - reproduced with both the npm codex.cmd shim and the native vendor codex.exe; one trivial-prompt test hung ~70min. The Bash tool gives codex a clean stdio env and works. Ran all 3 rounds via Bash (codex exec --model gpt-5.5 -c model_reasoning_effort=medium, prompt piped on stdin). The stdin pipe (now in invoke-codex-round.ps1) likely also closes stdin and may fix the pwsh hang - unverified this run. Consider documenting a Bash-driven fallback / verifying the script under the pwsh tool.
2026-05-28 dt-review RESOLVED in 0.9.26/0.9.27: stdin pipe in invoke-codex-round.ps1 fixed both the arg-length overflow and the PowerShell-tool hang (verified - the fixed script runs clean under the PowerShell tool in ~15s, not just Bash). SKILL.md Execution-model section mandates Bash + `pwsh -NoProfile -File` + per-step timeout; -NoProfile is the only deterministic guard against the separate ConstrainedLanguage profile crash, so Bash stays the canonical path. All tiers default to medium reasoning effort to override the global model_reasoning_effort=high.
2026-05-28 dt-review: a prior run failed with "gpt-5.1-codex-max model is not supported when using Codex with a ChatGPT account." Diagnosis - that run bypassed the scripts and hand-rolled codex calls (codex-out-p1a.txt/p1b) without --model, so it inherited the config default, which was the now-API-only gpt-5.1-codex-max. Live re-probe confirmed both current pins (gpt-5.5, gpt-5.3-codex) return OK. RESOLVED in 0.9.29: new shared scripts/resolve-codex-model.ps1 self-heals the pin against ~/.codex/models_cache.json (prefer pin -> per-tier fallback list -> throw with selectable list); preflight + invoke-codex-round gain -Tier and call it. VERIFIED 2026-05-28: resolver unit suite 10/10 pass (healthy-pin both tiers, dead-pin fallback both tiers, hidden-pin skip, missing/unparseable/zero-selectable soft-degrade, none-usable THROW with correct selectable list, no-pin tier-head); live preflight-codex.ps1 -Tier light ran clean (exit 0, resolved gpt-5.3-codex, returned OK). Tests written to a results file because the Bash tool's stdout rendering intermittently returned empty mid-session - read the file, do not trust a blank Bash result.
2026-07-12 dt-review: Codex rejected review-output-schema.json because structured outputs do not permit uniqueItems; moved candidate-dimension uniqueness enforcement into invoke-codex-round.ps1 semantic validation.
2026-07-12 dt-review: light review required repeated cap extensions because Rounds 3 and 4 surfaced one new low blocking coherence issue at a time, despite Round 4 explicitly instructing the reviewer to surface every establishable issue now rather than serialize findings.
2026-07-15 dt-review: gpt-5.6-sol failed complex Round 1 semantic validation twice by filling missing_evidence on ambiguous_root_cause=false findings (misusing the ambiguity-only field as a general evidence-gap note; a different finding tripped each attempt); the third identical invoke passed. Consider an explicit "missing_evidence stays empty unless AMBIGUOUS_ROOT_CAUSE" line in the prompt template/schema description.
2026-07-16 dt-review: finalize-review.ps1 requires the exact heading '## Build-intake revalidation' but nothing validates it at Round 0 intake; a FINALIZE_CURRENT run with a suffixed heading cannot be repaired in-flow (post-terminal confirmation must be byte-identical) and forced a full review restart
2026-08-11 dt-review: complex Round 1 (gpt-5.6-sol) blocked HARD by the same missing_evidence/ambiguous_root_cause=false trip logged 2026-07-15 - four consecutive invokes failed, so the 'third retry passes' workaround did not hold. Strengthening review-context.md with live evidence cut findings 18->12 and offending findings 4->3, but the residue is irreducible: the reviewer legitimately names counsel-approved governing-document interpretation as missing evidence, which no amount of code/DB inspection can close. Confirms the 2026-07-15 recommendation is now load-bearing: the prompt template/schema must state that missing_evidence stays empty unless AMBIGUOUS_ROOT_CAUSE, OR the validator must stop treating a populated missing_evidence on a non-ambiguous finding as fatal (it is useful reviewer signal, not a contract violation). Escalated to Danny rather than editing the skill through the junction mid-review.
2026-08-11 dt-review: RESOLVED in 1.7.1 (plugin 0.10.7). The missing_evidence/ambiguous_root_cause=false trip logged 2026-07-15 and again earlier today is fixed by stating the contract where the reviewer reads it - a paragraph in codex-prompt-template.md's structured-output section plus description fields on all three ambiguity properties in review-output-schema.json. The validator invariant is unchanged; the reviewer is simply told that missing_evidence answers 'which dimension' and not 'what the design lacks', and that an ordinary evidence gap belongs in root_cause/remediation. VERIFIED: all three dt-review suites pass (60/7/8 assertions), verify-versioning-policy.ps1 -BaseRef main returns pass:true, and the junction mirror at D:\Claude\skills\dt-review reports version 1.7.1 with both edits present.
2026-08-11 dt-review: THIRD bite of the '## Build-intake revalidation' trap (first logged 2026-07-16). A complex review converged NOTHING_TO_ADD at round 7 and could not be finalized: finalize-review.ps1 refuses a final design lacking that heading whenever review-context.md exists, and there is no in-flow repair - FINALIZE_CURRENT re-hashes draft-vN against the round receipt, authorize-next-round enforces a byte-identical post-terminal confirmation, and prepare-final-draft rejects FINALIZE_CURRENT outright. Recovery cost a full re-seed of the review (draft-v1 = converged design + the carried table), which then surfaced 9 genuine new findings over 6 more rounds - so the re-seed was not wasted, but the trap itself is pure process loss. FIXED in 1.7.2 (plugin 0.10.8): assemble-review-prompt.ps1 now warns every round, and returns build_intake_warning in its JSON, when review-context.md exists and the draft lacks the heading; input-modes.md states the requirement and why it cannot be repaired late. Advisory rather than a gate, since an early draft may legitimately not carry the table yet. VERIFIED: three-case unit test (warning fires / clears once the heading is added / silent when no review-context.md), all three dt-review suites pass (60/7/8), verify-versioning-policy pass:true, junction mirror reports 1.7.2.
2026-08-11 dt-review: FOURTH trap in the same family - a guardrail that hard-fails mid-review with no in-flow recovery. assemble-review-prompt.ps1 threw above a fixed 250000-byte prompt at round 8 of a converging complex review. Cumulative state is the growth driver (verdicts.json alone reached 137KB) and it is script-managed with SHA receipts, so the only compactable input is review-context.md - already trimmed 18.3KB to 8.4KB, which was not enough. Neither continue nor finalize was reachable. FIXED in 1.8.0 (plugin 0.11.0): the limit is now a configurable budget defaulting to 900000 bytes (~225k tokens; the round in question used 77,610 tokens, so the old ceiling was ~3x tighter than any real model constraint), overridable via -MaxPromptBytes or DT_REVIEW_MAX_PROMPT_BYTES, with an 80% warning and prompt_budget_bytes/prompt_budget_warning in the JSON so growth is visible while review-context.md can still absorb it. VERIFIED: 5-case test (300KB draft that the old limit rejected now assembles; explicit tight budget throws; env override throws; 80% warning fires; malformed env value rejected), all three suites pass (60/7/8), verify-versioning-policy pass:true. Pattern worth noting across 1.7.1/1.7.2/1.8.0: dt-review's gates are correct in intent but several were unrecoverable once tripped; prefer warn-early-then-block over block-only.
2026-08-14 dt-review: Mode A plan authored directly by the architect (per the skip-dt-plan rule) had no shape_version frontmatter and used numbered '## 5. Open questions' / '## 8. Out of scope' headings instead of the exact required ones; proceeded non-interactively as a subagent rather than stalling on the soft contract.
