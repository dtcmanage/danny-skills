Append-only friction notes. One line per invocation that hit friction.
2026-05-27 dt-roadmap: build-roadmap.ps1 fails on finalized designs that use section-based headings instead of `### Phase ...`, requiring manual roadmap authoring before validator/export.
2026-05-27 dt-roadmap (v1.1.0): producer now parses `## Implementation Sequence` numbered lists as primary milestone source (legacy `### Phase` retained as fallback), and lifts milestone-tagged backticked commands from `## Validation Gates` verbatim into procedure cells. Validator now enforces VERIFICATION_NAMES_NO_RUNNABLE through repo-level `scripts/extract-named-artifacts.ps1` (byte-shared with dt-build's gate). Producer + validator now stay in lockstep with dt-build per-milestone acceptance gate (the 2026-05-27 producer/consumer drift is closed).
2026-07-15 dt-roadmap: reviewed dt-review output used `Milestones and dependency order` with `M00`-`M07` headings and no `Validation Gates`, so the deterministic producer required a provenance-bound adapter; producer and review output contracts are not aligned.
2026-08-31 dt-roadmap: adapter/producer emitted an unrunnable verification command (bare browser-smoke smoke.mjs without --url, which the harness hard-requires) — surfaced only at M08 gate time; needed an approved contract revision (r1). Producer could sanity-check that named commands are invocable as written.
